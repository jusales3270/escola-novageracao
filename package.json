{
  "name": "somaescola",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "build": "pnpm -r --if-present --filter=./packages/* run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm --filter @somaverso/schemas test && pnpm --filter @somaverso/motor test && pnpm --filter @somaverso/ledger test",
    "test:coverage": "pnpm --filter @somaverso/motor test:coverage",
    "test:integration": "pnpm --filter @somaverso/ledger test:integration",
    "test:integration:full": "pnpm db:up && pnpm db:migrate && pnpm test:integration",
    "lint": "pnpm -r --if-present run lint",
    "db:up": "docker compose up -d --wait db",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && pnpm db:up && pnpm db:migrate",
    "db:migrate": "pnpm --filter @somaverso/db run migrate"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
