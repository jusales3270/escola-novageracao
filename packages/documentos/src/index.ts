// PRD §11 — templates HTML + CSS de impressão (requerimento, contrato),
// renderizados por Playwright/Chromium. Implementação real em M4.

export function renderRequerimento(): never {
  throw new Error("Não implementado — M4");
}

export function renderContrato(): never {
  throw new Error("Não implementado — M4");
}
